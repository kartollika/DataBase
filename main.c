#include <stdio.h>
#include <stdlib.h>
#include "headers/file_inout.h"

int main()
{
    participant *participants = NULL;

    FILE *f = NULL;
    f = initFILE(f);


    closeFILE(f);

    return 0;
}
